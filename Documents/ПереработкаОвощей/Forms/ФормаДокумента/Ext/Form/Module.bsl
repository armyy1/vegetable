&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	Если Не ЗначениеЗаполнено(Объект.Ссылка) Тогда
		ВыборкаМестХранения = Справочники.МестаХранения.Выбрать();
		Если ВыборкаМестХранения.Следующий() Тогда
			Объект.СкладИсточник = ВыборкаМестХранения.Ссылка;
			Объект.СкладПриемник = ВыборкаМестХранения.Ссылка;
		КонецЕсли;
		Объект.Ответственный = ПараметрыСеанса.ТекущийПользователь;
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура ТоварыКоличествоСырьеПриИзменении(Элемент)
	ПересчитатьСтрокуТоваров();
КонецПроцедуры

&НаКлиенте
Процедура ТоварыКоличествоПотерьПриИзменении(Элемент)
	ПересчитатьСтрокуТоваров();
КонецПроцедуры

&НаКлиенте
Процедура ПересчитатьСтрокуТоваров()
	СтрокаТабличнойЧасти = Элементы.Товары.ТекущиеДанные;
	Если СтрокаТабличнойЧасти = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если Не ЗначениеЗаполнено(СтрокаТабличнойЧасти.КоличествоСырье) Тогда
		СтрокаТабличнойЧасти.КоличествоСырье = 0;
	КонецЕсли;
	Если Не ЗначениеЗаполнено(СтрокаТабличнойЧасти.КоличествоПотерь) Тогда
		СтрокаТабличнойЧасти.КоличествоПотерь = 0;
	КонецЕсли;
	
	КоличествоПродукции = СтрокаТабличнойЧасти.КоличествоСырье - СтрокаТабличнойЧасти.КоличествоПотерь;
	Если КоличествоПродукции < 0 Тогда
		КоличествоПродукции = 0;
	КонецЕсли;
	
	СтрокаТабличнойЧасти.КоличествоПродукция = КоличествоПродукции;
КонецПроцедуры
